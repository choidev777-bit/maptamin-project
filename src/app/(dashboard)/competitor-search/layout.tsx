
export default function CompetitorSearchLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <div className="min-h-full">
            {children}
        </div>
    )
}
