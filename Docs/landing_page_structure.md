# 맵타민 랜딩 페이지 구조 정리

> **파일 위치**: `src/components/landing/`
> **최종 정리일**: 2026-02-11

---

## 브랜드 컬러

| 구분 | 컬러 코드 | 용도 |
|------|-----------|------|
| 메인 그린 (Teal Green) | `#00C896` | CTA 버튼, 강조 텍스트, 뱃지, 초록불 |
| 호버 그린 | `#00B386` | 버튼 호버 시 |
| 딥 네이비 | `#002959` | 로고 그리드 (MaptaminLogo) |
| 위험 레드 | `#EF4444` / `red-500` | 빨간불, 경고 표시 |
| 주의 옐로우 | `#EAB308` / `yellow-400` | 노란불, 주의 표시 |
| 안전 그린 | `#22C55E` / `green-500` | 히어로 도트맵 초록불 |

---

## 컴포넌트 목록 (페이지 순서)

| # | 컴포넌트 | 파일명 | 타입 |
|---|----------|--------|------|
| 0 | 맵타민 로고 | `MaptaminLogo.tsx` | 서브 컴포넌트 |
| 0 | 1/50 분수 아이콘 | `FractionOneFifty.tsx` | 서브 컴포넌트 |
| 1 | 네비게이션 | `Navigation.tsx` | Server Component |
| 2 | 히어로 섹션 | `HeroSection.tsx` | Server Component |
| 3 | 문제 제기 섹션 | `ProblemSection.tsx` | Server Component |
| 4 | 기능 소개 섹션 | `FeatureSection.tsx` | Server Component |
| 5 | 경쟁사 비교 섹션 | `SocialProofSection.tsx` | Server Component |
| 6 | 가격 섹션 | `PricingSection.tsx` | Client Component (`'use client'`) |
| 7 | 푸터 | `Footer.tsx` | Server Component |

---

## 0. 서브 컴포넌트

### 0-1. MaptaminLogo (`MaptaminLogo.tsx`)

- **구조**: 3×3 SVG 그리드 아이콘 + "Maptamin" 텍스트
- **아이콘**: 정사각형 9개, 중앙만 `#00C896` (틸그린), 나머지 8개 `#002959` (딥네이비)
- **텍스트**: "Maptamin", font-bold, `#002959` 색상
- **반응형**: 모바일 `h-10`, `text-xl` → SM 이상 `h-12`, `text-2xl`

### 0-2. FractionOneFifty (`FractionOneFifty.tsx`)

- **용도**: 가격 섹션 타이틀에서 "1/50 가격" 표현 시 사용
- **구조**: SVG로 분수 표현 (위: "1", 가로선, 아래: "50")
- **색상**: `currentColor` (부모에서 상속)

---

## 1. 네비게이션 (`Navigation.tsx`)

**스타일**: Sticky 상단 고정, 반투명 배경 (`bg-white/80 backdrop-blur-lg`), 하단 보더

**레이아웃**: 좌우 `justify-between`
- **좌측**: `MaptaminLogo` 컴포넌트 (Link → `/`)
- **우측**: "로그인" CTA 버튼 (Link → `/login`)
  - 스타일: `rounded-xl`, `bg-[#00C896]`, 흰색 텍스트
  - 호버: `-translate-y-0.5`, `bg-[#00B386]`, `shadow-md`

---

## 2. 히어로 섹션 (`HeroSection.tsx`)

**배경 장식**:
- 우측 그라데이션: `from-[#00C896]/5 to-transparent`
- 우상단 원형 블러: `bg-[#00C896]/10 blur-3xl`
- 좌측 중앙 원형 블러: `bg-blue-400/5 blur-3xl`
- 그리드 배경 패턴: 20px × 20px 연한 초록 라인

**레이아웃**: `lg:flex-row` 2단 구성 (모바일: 세로 쌓임)

### 좌측 (60% 너비)

- **Eyebrow 뱃지**:
  - 텍스트: "우리 가게 지도 건강검진, 맵타민"
  - 스타일: `rounded-full`, `bg-[#00C896]/10`, 초록 텍스트
  - 장식: 펄싱 초록 도트 (`animate-ping`)

- **타이틀 (H1)**:
  ```
  우리 가게
  '진짜 순위'를 알아야
  '진짜 매출'이 오릅니다.
  ```
  - `'진짜 순위'` → `text-[#00C896]` 초록색 강조
  - `'진짜 매출'` → 아래 SVG 곡선 밑줄 장식 (`#00C896/30`)
  - 폰트: `font-extrabold`, 반응형 (`text-3xl` → `xl:text-5xl`)

- **서브텍스트**:
  ```
  [플레이스 순위 지도]로 지도 위 '빨간불'을 찾으세요.
  초록불로 바꾸는 순간, 놓치던 매출이 내 것이 됩니다.
  ```
  - `[플레이스 순위 지도]` → `<strong>` bold gray-700
  - `'빨간불'` → `text-red-500`
  - `초록불` → `text-[#00C896]`

- **CTA 버튼**: "우리 가게 '진짜 순위' 확인하기" (Link → `/login`)
  - 스타일: `rounded-full`, `bg-[#00C896]`, 흰색 bold
  - ArrowRight 아이콘 (호버 시 오른쪽 이동)
  - 그림자: `shadow-[0_20px_40px_-10px_rgba(0,199,149,0.3)]`

### 우측 (50% 너비) — 지도 목업 카드

- **플로팅 Rank 카드**: 우상단 위치, `animate-bounce` (3초 주기)
  - 내용: "순위" 라벨 + "#1" + TrendingUp 아이콘 (초록)

- **메인 카드**: 브라우저 크롬 스타일
  - **브라우저 크롬 바**: 빨강/노랑/초록 3개 원형 도트 + 회색 주소창 바
  - **지도 영역** (4:3 비율):
    - 배경: 그라데이션 (`green-50 → blue-50 → gray-100`) + 그리드 패턴
    - **5×7 도트 그리드**: `DOT_COLORS` 데이터 기반
      - 초록 `#22C55E`: 좌상단 영역 (안전 구역)
      - 노랑 `#EAB308`: 중간 경계 영역 (주의)
      - 빨강 `#EF4444`: 우하단 영역 (위험)
      - 중앙 도트: `scale-125`, `border-white`, `animate-pulse`
      - 각 도트에 글로우 효과 (`boxShadow`) 적용
    - **중앙 핀 라벨**: "내 가게" 말풍선 + 삼각형 포인터
  - **하단 오버레이**: "지도 건강 점수" 카드
    - Shield 아이콘 + "우수 (92/100)" 텍스트
    - 우측 스파크라인 SVG 차트

---

## 3. 문제 제기 섹션 (`ProblemSection.tsx`)

2개의 하위 블록으로 구성.

### 3-1. 경고 섹션 (Problem)

**배경**: 흰색, `skew-y-3` 기울어진 회색 배경 레이어

- **헤더**:
  - Eyebrow 뱃지: `⚠ WARNING` (빨간 배경, 대문자)
  - 타이틀 (H2):
    ```
    사장님의 플레이스,
    내일이면 사라질 수도 있습니다.
    ```
    - 두 번째 줄 `text-red-500`

- **공지사항 경고 카드**: 라운드 카드, `shadow-xl`, 호버 시 `-translate-y-1`
  - **빨간 헤더 바**: AlertTriangle 아이콘 + "[네이버 스마트플레이스 공지사항]"
  - **본문 리스트** (3개 항목):
    1. 허위 클릭을 과다하게 유발하는 경우
    2. 가짜 영수증을 통한 허위 리뷰
    3. 마케팅 대행사, 리워드 서비스 등을 통한 허위 리뷰 작성 등
  - **하단 경고 문구**: "위 어뷰징 행위가 **1회**만 확인되더라도, **페널티**를 즉각 적용!" (빨간 텍스트)

### 3-2. 솔루션 섹션 (Solution)

**배경**: `bg-slate-50`, 상단 보더

**레이아웃**: `lg:grid-cols-2` 2단 (모바일: 인용 카드 먼저 → 텍스트)

- **좌측 텍스트**:
  - 타이틀 (H3): "정답은 **'진짜 인기'**에 있습니다." (`'진짜 인기'` → 초록색)
  - 서브텍스트: "숨어있는 **빨간불**을 찾아 공략하세요. **초록불**이 늘어날수록 **'지역 1등'**에 가까워집니다."

- **우측 인용 카드**:
  - 배경 블러 장식: 초록 그라데이션
  - Megaphone 아이콘 + "Official Guide" 라벨
  - 인용문 (blockquote):
    ```
    "네이버는 공식적으로 인기도가 높으면 거리가 멀어도 상위 노출된다고 밝혔습니다."
    ```
    - `인기도가 높으면` → `#00C896/30` 밑줄 장식
  - 출처: "출처: 네이버 스마트플레이스 공식 가이드"
  - Quote 아이콘 (우하단)

---

## 4. 기능 소개 섹션 (`FeatureSection.tsx`)

3개의 하위 블록으로 구성. Fragment (`<>`) 래핑.

### 4-1. 신호등 시스템 설명

**레이아웃**: `lg:grid-cols-2` 2단

**좌측 — 텍스트 & 신호등 설명**:
- Eyebrow 뱃지: "Smart Analysis" (초록 `rounded-full`)
- 타이틀 (H2): "데이터 분석, 몰라도 괜찮습니다. **초록색**과 **빨간색**만 구분하세요."

- **신호등 3개 설명**:

  | 신호 | 아이콘 | 순위 범위 | 설명 |
  |------|--------|-----------|------|
  | 🟢 초록불 (안전) | CheckCircle2 (초록) | 1~5위 | 완벽하게 장악한 '내 구역'. 매출 발생 안전지대 |
  | 🟡 노란불 (주의) | AlertTriangle (노랑) | 6~10위 | 조금만 밀어붙이면 '내 구역'. 가성비 좋은 기회 좌표 |
  | 🔴 빨간불 (위험) | Flame (빨강) | 10위 밖 | 경쟁사에게 손님 뺏김. 즉각 조치 필요 경고 |

  - 각 아이콘 `group-hover:scale-110` 호버 효과

**우측 — 히트맵 시각화 카드**:
- 배경 블러 장식 (초록 그라데이션)
- 지도 이미지 배경 (흑백, `grayscale opacity-20`)
- **브라우저 크롬**: 빨강/노랑/초록 도트 + "Live Analysis Tracking" 라벨
- **7×7 그리드** (49개 도트):
  - 중앙 (#24): `bg-[#00C896]` + `ring-4 ring-white shadow-xl animate-pulse` (내 가게)
  - 중앙 주변 8개: `bg-[#00C896]/60` (주변 초록)
  - 중간대 16개: `bg-yellow-400/60` (노랑)
  - 외곽: `bg-red-500/40` (빨강)
  - 모서리 4개: `bg-red-500/80` (진한 빨강, 위험)

### 4-2. 왜 맵타민이어야 할까요? (장점 3가지)

**배경**: `bg-[#f5f8f8]`

- 타이틀 (H2): "왜 맵타민이어야 할까요?"
- **3 컬럼 카드 그리드** (`md:grid-cols-3`):

  | # | 아이콘 | 제목 | 설명 |
  |---|--------|------|------|
  | 1 | BarChart3 | 이해하기 쉬운 신호등 시스템 | 초록불, 노란불, 빨간불로 위치별 진짜 순위 한눈에 파악 |
  | 2 | ClipboardCheck | 팩트 체크 | 마케팅 대행사 성과 감시. 지도 색깔 실시간 변화로 투명한 성과 확인 |
  | 3 | Map | 경쟁사 땅따먹기 | 경쟁 업체 강/약점 지역 파악, 비어있는 시장 공략 |

  - 카드 스타일: `rounded-2xl`, `border-slate-100`, `shadow-sm`
  - 호버: `-translate-y-2`, `shadow-xl`
  - 아이콘: `bg-[#00C896]/10` 배경, 초록색

### 4-3. 알림 서비스

**레이아웃**: `lg:grid-cols-2` 2단

**좌측 텍스트**:
- 타이틀 (H2): "매일 접속하지 않으셔도 됩니다."
- 서브텍스트: "바쁜 사장님을 위해 맵타민이 직접 찾아갑니다."

**우측 카드 리스트** (2개):

| # | 아이콘 | 제목 | 태그 | 설명 |
|---|--------|------|------|------|
| 1 | MessageCircle (초록) | 띵동! 배달 왔습니다 | (주간 보고서) | 사장님이 가장 한가한 시간에 매주 카톡으로 '플레이스 순위 지도' 발송 |
| 2 | Zap (노랑) | 지금 당장 확인하고 싶다면? | (실시간 티켓) | '실시간 진단 티켓'으로 지금 이 순간의 순위 즉시 조회 |

- 카드 스타일: `rounded-2xl`, `bg-slate-50`, `border-slate-100`
- 호버: `bg-white`, `shadow-lg`
- 아이콘 호버: `group-hover:rotate-12`

---

## 5. 경쟁사 비교 섹션 (`SocialProofSection.tsx`)

**배경**: `bg-[#f8fafc]`

**레이아웃**: `lg:grid-cols-2` 2단

### 좌측 — 텍스트 & 특징

- 타이틀 (H2): "똑똑한 사장님들은 '**맵타민**'으로 한 주를 시작하고 있습니다."
  - `맵타민` 초록색 강조
- 특징 리스트: "✓ 상세 구역별 승/패 분석" (CheckCircle2 아이콘)
- CTA 버튼: "경쟁사 분석 시작하기" + ArrowRight
  - 스타일: ghost 버튼 (`border-slate-200`, `bg-white`)
  - 호버: `border-[#00C896]/50`, `bg-[#00C896]/5`

### 우측 — 경쟁사 비교 비주얼 카드

- 배경 블러 장식: 회색 그라데이션
- **카드 헤더**:
  - "경쟁사 비교 분석" 제목
  - 탭 전환: "내 가게" (활성, 흰배경+초록) / "경쟁사 A" (비활성)
  - 범례: 🟢 승(Win) / 🔴 패(Loss)

- **5×5 비교 그리드**: `COMPARISON_GRID` 데이터 기반
  - 중앙: 원형 "나" 마커 (`bg-[#00C896]`, 흰 테두리, `shadow-xl`)
  - 승(win): 초록 원형 `bg-[#00C896]`, "승" 텍스트
  - 패(lose): 빨간 원형 `bg-red-500`, "패" 텍스트
  - 호버: `scale-110`

  **그리드 배치**:
  ```
  승 승 패 패 패
  승 승 승 패 패
  승 승 나 승 승
  패 패 승 승 승
  패 패 승 승 승
  ```

---

## 6. 가격 섹션 (`PricingSection.tsx`)

**타입**: Client Component (`'use client'`)
**상태**: `isYearly` (월간/연간 토글)
**배경**: `bg-white`

### 섹션 타이틀

- H2: "마케팅 대행사 & 분석 앱 비용의 **1/50 가격** '핵심'만 남겼습니다."
  - `1/50` → `FractionOneFifty` SVG 컴포넌트 (초록)
- 서브텍스트: "하루 900원대로 사장님의 플레이스 순위를 지키세요."

### 월간/연간 토글 스위치

- 좌측: "월간" / 우측: "연간" + `1개월 무료` 초록 뱃지
- 토글 스타일: `rounded-full`, 활성 시 `bg-[#00C896]`

### 3개 가격 플랜 카드 (`lg:grid-cols-3`)

| 항목 | 스타터 | 프로 ⭐ (추천) | 프리미엄 |
|------|--------|---------------|----------|
| **태그라인** | 1인 매장 사장님용 | 대행사 검증 & 경쟁사 분석용 | 상권 장악에 진심인 사장님용 |
| **월간 가격** | 9,900원/월 | 29,000원/월 | 99,000원/월 |
| **연간 가격** | 9,075원/월 (연 108,900원) | 26,600원/월 (연 319,000원) | 90,750원/월 (연 1,089,000원) |
| **뱃지** | — | "회원 83%가 구독 중" | — |
| **지도 진단** | 네이버 지도 | 네이버 지도 | 네이버 + 구글 지도 |
| **좌표 분석** | 3×3 (9개) | 5×5 (25개) | 7×7 (49개) |
| **관리 키워드** | 2개 | 5개 | 10개 |
| **주간 보고서** | ✓ | ✓ | ✓ |
| **실시간 티켓** | 월 2회 | 월 10회 | 월 30회 |
| **경쟁사 분석** | ✕ 불가 | 1곳 집중 추적 | 10곳 심층 분석 |
| **연결 가게 변경** | — | — | 무제한 |
| **CTA 버튼** | "가볍게 시작하기" (ghost) | "내 가게 진단 & 경쟁사 분석 시작하기" (solid) | "상권 완전 장악하기" (ghost) |

- **추천 카드 (프로)**: `scale-105`, `border-[#00C896]`, `shadow-2xl`, 상단 뱃지
- **공통**: "🚀 결제 즉시 첫 보고서 발송" 문구, "VAT 별도" 표시

---

## 7. 푸터 (`Footer.tsx`)

**배경**: `bg-white`, 상단 보더

**레이아웃**: 3단 구조 (세로)

### 상단 영역 (로고 + 링크)
- **좌측**: 로고 이미지 (`logo_v3.png`, `h-8`) + "우리 가게 지도 건강검진 서비스" 텍스트
- **우측**: 링크 2개
  - "이용약관" → `/terms`
  - "개인정보처리방침" → `/privacy`

### 중단 영역 (사업자 정보)
- 상호: **아카식 허브**
- 대표: 최연준
- 사업자등록번호: 186-35-01741
- 통신판매업신고: 제 2026-고양일산서-0229 호
- 주소: 경기도 고양시 일산서구 대산로 142, 305동
- 개인정보관리책임자: 최연준

### 하단 영역 (저작권)
- "© 2026 맵타민 (Maptamin). All rights reserved." (동적 연도)

---

## 사용 아이콘 라이브러리

**lucide-react** 아이콘 목록:

| 아이콘 | 사용 위치 |
|--------|-----------|
| `ArrowRight` | 히어로 CTA, SocialProof CTA |
| `TrendingUp` | 히어로 Rank 카드 |
| `Shield` | 히어로 지도 건강 점수 |
| `AlertTriangle` | ProblemSection 경고, FeatureSection 노란불 |
| `Megaphone` | ProblemSection 인용 카드 |
| `Quote` | ProblemSection 인용 카드 |
| `CheckCircle2` | FeatureSection 초록불, SocialProof 특징 |
| `BarChart3` | FeatureSection 장점 카드 |
| `ClipboardCheck` | FeatureSection 장점 카드 |
| `Map` | FeatureSection 장점 카드 |
| `MessageCircle` | FeatureSection 알림 서비스 |
| `Zap` | FeatureSection 알림 서비스 |
| `Flame` | FeatureSection 빨간불 |

---

## 반응형 브레이크포인트 전략

| 브레이크포인트 | 주요 변화 |
|----------------|-----------|
| 기본 (모바일) | 1 컬럼, 세로 쌓임, 텍스트 중앙 정렬 |
| `sm` (640px) | 텍스트 크기 증가, 버튼 가로 배치 |
| `md` (768px) | 장점 카드 3 컬럼, 일부 줄바꿈 표시 |
| `lg` (1024px) | 2 컬럼 레이아웃 활성화, 텍스트 좌측 정렬, 가격 3 컬럼 |
| `xl` (1280px) | 타이틀 최대 크기 (`text-5xl`) |
